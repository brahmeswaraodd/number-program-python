def is_even(num):
    if num % 2 == 0:
        return True
    else:
        return False

num = int(input("Enter a number: "))

if is_even(num):
    print(f"{num} is even.")
else:
    print(f"{num} is odd.")
